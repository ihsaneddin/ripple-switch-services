<%= yield_content_for :page_title, "New Wallet" %>


<% if modal_params.present? %>
  <%= form_for @wallet, url: ripple_wallet_path(@wallet), html: { class: "form form-horizontal" }, remote: true, method: :put do |f| %>

    <%= render :partial => "ripple/wallets/form", locals: { wallet: @wallet,f: f } %>

    <%= modal_input_params %>
    <%= table_input_params %>

  <%-end %> 
<%- end %>
